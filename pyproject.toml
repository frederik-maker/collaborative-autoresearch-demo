[project]
name = "agi-threshold-sim"
version = "0.2.0"
description = "Four-agent geopolitical simulation on the AXL P2P mesh. Forked from gensyn-ai/collaborative-autoresearch-demo."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "anthropic>=0.40.0",
    "flask>=3.0.0",
]

[tool.uv]
package = false
